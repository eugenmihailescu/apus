{% if post.tags is not empty %}
    <div>
        {% for tag in post.tags %}
            <a href="#{{tag.slug}}" class="badge badge-warning">{{tag.name}}</a>
        {% endfor %}
    </div>
{% endif %}